# Database (add your own)
# DATABASE_URL=sqlite:///./traceroots.db

# TraceRoots blockchain (traceroots-blockchains)
# 1. Run: cd traceroots-blockchains && npx hardhat node
# 2. Deploy: npx hardhat run scripts/deploy.js --network localhost
# 3. Copy contract address below and fund the deployer account
RPC_URL=http://127.0.0.1:8545
CONTRACT_ADDRESS=
PRIVATE_KEY=
